import { colors } from "@/styles/constants";

export const headerStyles = {
  transparentHeaderItem: {
    color: colors.white,
    cursor: "pointer",
    textDecoration: "none",
    "&:hover": {
      borderBottom: "1px solid",
    },
  },
};
